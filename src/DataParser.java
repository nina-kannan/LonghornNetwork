import java.io.*;
import java.util.*;

/**
 * DataParser provides utility methods for parsing student data from files.
 */
public class DataParser {
    /**
     * Parses a list of UniversityStudent objects from the specified file.
     *
     * @param filename the name of the file containing student data
     * @return a list of UniversityStudent objects
     * @throws IOException if an I/O error occurs while reading the file
     */
    public static List<UniversityStudent> parseStudents(String filename) throws IOException {
        return new ArrayList<>();
    }
}
